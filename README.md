# Skin-Cancer-Classification-Convolutional-Neural-Network
A convolutional neural network (CNN) project for classifying seven types of skin lesions using the HAM10000 dataset. The project includes data exploration, preprocessing, class balancing, image classification, model training, and performance evaluation using accuracy, confusion matrices, and misclassification analysis.
